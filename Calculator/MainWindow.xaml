<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        mc:Ignorable="d"
        Title="Калькулятор" 
        
        Height="525" 
        Width="315"
        MinHeight="525" 
        MaxHeight="525"
        MinWidth="315" 
        MaxWidth="315"
        ResizeMode="CanMinimize"
        
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <Style x:Key="MainButton" TargetType="Button">
            <Style.Setters>
                <Setter Property="Background" Value="WhiteSmoke"/>
                <Setter Property="BorderBrush" Value="LightGray"/>
                <Setter Property="BorderThickness" Value="1"/>

                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="DemiBold"/>

                <Setter Property="Margin" Value="1"/>
                <Setter Property="Width" Value="70"/>
                <Setter Property="Height" Value="50"/>

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style.Setters>

            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="LightGray"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="RenderTransformOrigin" Value="0.5, 0.5"/>
                </Trigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightGray"/>
                </Trigger>
            </Style.Triggers>

        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel>
            <Border Height="30"/>
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                          Height="100">
                <TextBox x:Name="Display" 
                           Padding="25" 
                           TextAlignment="Left" Text="0" FontSize="30"/>
            </ScrollViewer>
            <WrapPanel x:Name="Buttons" Button.Click="Button_Click" Width="288">
                <Button Content="x" Style="{StaticResource MainButton}"/>
                <Button Content="y" Style="{StaticResource MainButton}"/>
                <Button Content="z" Style="{StaticResource MainButton}"/>
                <Button Content="←" Style="{StaticResource MainButton}"/>
                <Button Content="(" Style="{StaticResource MainButton}"/>
                <Button Content=")" Style="{StaticResource MainButton}"/>
                <Button Content="^" Style="{StaticResource MainButton}"/>
                <Button Content="÷" Style="{StaticResource MainButton}"/>
                <Button Content="7" Style="{StaticResource MainButton}"/>
                <Button Content="8" Style="{StaticResource MainButton}"/>
                <Button Content="9" Style="{StaticResource MainButton}"/>
                <Button Content="×" Style="{StaticResource MainButton}"/>
                <Button Content="4" Style="{StaticResource MainButton}"/>
                <Button Content="5" Style="{StaticResource MainButton}"/>
                <Button Content="6" Style="{StaticResource MainButton}"/>
                <Button Content="－" Style="{StaticResource MainButton}"/>
                <Button Content="1" Style="{StaticResource MainButton}"/>
                <Button Content="2" Style="{StaticResource MainButton}"/>
                <Button Content="3" Style="{StaticResource MainButton}"/>
                <Button Content="＋" Style="{StaticResource MainButton}"/>
                <Button Content="C" Style="{StaticResource MainButton}"/>
                <Button Content="0" Style="{StaticResource MainButton}"/>
                <Button Content="," Style="{StaticResource MainButton}"/>
                <Button Content="=" Style="{StaticResource MainButton}"/>
                <Button x:Name="equal" Content="Вычислить!"
                        Background="CornflowerBlue" BorderBrush="CornflowerBlue" Foreground="White" 
                        BorderThickness="0"
                        Width="284" Height="40" Margin="2"
                        Style="{StaticResource MainButton}">
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Режим вычислений">
                                <MenuItem Header="ПОЛИЗ"/>
                                <MenuItem Header="РЕКУРСИЯ"/>
                            </MenuItem>
                        </ContextMenu>
                    </Button.ContextMenu>
                    <Button.ToolTip>
                        <ToolTip Content="← Выберите режим" 
                                 Placement="Right" HorizontalOffset="5" VerticalOffset="-10"/>
                    </Button.ToolTip>
                </Button>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
